(smusni-lexical-fixtures 1
  ;; These rows are non-normative scaffolding until issue #12 supplies the
  ;; adjudicated lexical resource. `required` is the number of positional
  ;; fills needed for a Content result in this bounded checker; remaining
  ;; places are represented by an open PredTerm and closed explicitly.
  (row bajra 1 5 direct-event "fixture; official row pending #12")
  (row barda 1 2 holding-state "fixture; official row pending #12")
  (row batci 2 3 direct-event "fixture; official row pending #12")
  (row blabi 1 1 holding-state "fixture; official row pending #12")
  (row cabna 2 3 holding-state "fixture; official row pending #12")
  (row cadzu 1 4 direct-event "fixture; official row pending #12")
  (row cinri 1 2 holding-state "fixture; official row pending #12")
  (row citka 1 3 direct-event "fixture; official row pending #12")
  (row cukta 1 5 holding-state "fixture; official row pending #12")
  (row cusku 2 4 direct-event "fixture; official row pending #12")
  (row darxi 2 3 direct-event "fixture; official row pending #12")
  (row datka 1 1 holding-state "fixture; official row pending #12")
  (row djica 2 3 holding-state "fixture; official row pending #12")
  (row djuno 2 4 holding-state "fixture; official row pending #12")
  (row fasnu 1 1 holding-state "fixture; official row pending #12")
  (row flulimna 1 1 direct-event "fixture; official row pending #12")
  (row gerku 1 1 holding-state "fixture; official row pending #12")
  (row jbena 1 3 direct-event "fixture; official row pending #12")
  (row jbopre 1 1 holding-state "fixture; official row pending #12")
  (row jinvi 2 4 holding-state "fixture; official row pending #12")
  (row jmaji 1 1 direct-event "fixture; official row pending #12")
  (row klama 1 5 direct-event "fixture; official row pending #12")
  (row melbi 1 2 direct-event "fixture; official row pending #12")
  (row mlatu 1 1 holding-state "fixture; official row pending #12")
  (row morsi 1 1 direct-event "fixture; official row pending #12")
  (row nelci 2 2 holding-state "fixture; official row pending #12")
  (row pendo 1 2 holding-state "fixture; official row pending #12")
  (row pilno 2 3 holding-state "fixture; official row pending #12")
  (row pipno 1 1 holding-state "fixture; official row pending #12")
  (row ponse 2 2 holding-state "fixture; official row pending #12")
  (row prami 2 2 holding-state "fixture; official row pending #12")
  (row prenu 1 1 holding-state "fixture; official row pending #12")
  (row purci 2 2 holding-state "fixture; official row pending #12")
  (row rinka 2 3 holding-state "fixture; official row pending #12")
  (row selcmi 2 2 holding-state "fixture; official row pending #12")
  (row sipna 1 1 direct-event "fixture; official row pending #12")
  (row skicu 4 4 direct-event "fixture; official row pending #12")
  (row srana 2 2 holding-state "fixture; official row pending #12")
  (row stali 1 1 direct-event "fixture; official row pending #12")
  (row sutra 1 1 holding-state "fixture; official row pending #12")
  (row tatpi 1 1 holding-state "fixture; official row pending #12")
  (row tavla 2 3 direct-event "fixture; official row pending #12")
  (row valsi 1 2 holding-state "fixture; official row pending #12")
  (row xasli 1 1 holding-state "fixture; official row pending #12")
  (row zdani 2 2 holding-state "fixture; official row pending #12"))
