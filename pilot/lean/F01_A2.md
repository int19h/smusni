# F01-A2 — scope-accurate non-admission evidence

Base: `8c14eaabdfca0866fefa253792b96fdf127b70b1`. PM batch
`msg_20260911T222059013436Z_b394a90f97ab4f1aa4bf50bf869a700b` confirms Astra's
finding `msg_20260911T222012065591Z_93a8d5cef7ea44d498b966e8c1c35d5e`.
The semantic source remains `3e45db5`; no normative document, runtime,
normalizer, deep typing or proof rule changes in this correction.

## Mechanism

The oracle exporter receives the grouped adapter form, in which literal Host
and Assert markers have already been validated/normalized. Its free-variable
walk now recognizes PerformSource and visits S in the incoming environment,
C1 with only its first binder added, and D with only read/o added. Binder/type
descriptors are not walked as term occurrences. Each arm has an independent
environment; outer bindings and lexical shadowing are retained.

Scope validation still precedes unsupported-context routing. Actual missing
declarations and illegal cross-arm uses therefore remain malformed-input
errors. Once scope succeeds, PerformSource is a named unsupported context:
the exporter does not enter A0 typing or target expansion for it. No case ID
selects this behavior and no binder is globally declared.

The source-only producer uses the same admission boundary and now retains
category, stage and reason instead of discarding them. This explicit evidence
change is versioned as `smusni-m2-oracle-sources 2`; the current consumer rejects
the old version rather than silently accepting evidence-less failures.
All 82 typed-source witnesses remain unchanged. All 113 source-level failures
now carry independently produced diagnostic evidence. When the source-only
contract reports failure, the consumer requires the candidate oracle's category,
stage and reason to agree; source admission followed by target-expansion failure
remains a distinct, permitted case.

## Exact evidence changes

These three retained definition candidates change from
`malformed-input / environment / missing declaration for free variable $x`
to `context-unsupported / context / no independent context rule for
PerformSource within whole-source`:

- `4601e0fc1cd98dda4396bef40b0909f12e3e35b0`
- `679a6c30ff1a50022b0318f997405b963275d5ec`
- `eb25b474d04d551d558e517f0248943c27c6712e`

| Oracle category | Before | After |
|---|---:|---:|
| Available | 74 | 74 |
| Context unsupported | 71 | 74 |
| Grammar unsupported | 25 | 25 |
| Unclassified non-admission | 22 | 22 |
| Malformed input | 3 | 0 |

The cohort remains 195 and the corpus remains 421. The same 54 whole-A0 and
20 Assert-bridge targets retain identical terms and typing evidence. No new
oracle scope or admission is claimed. `F01_A2_MIGRATION.json` compares every
old/current record, checks all 74 available records for exact preservation,
and verifies that corpus, cohort and benchmark bytes are unchanged.
The prior records remain in the immutable reviewed parent commit.

## Validation

- `raco test pilot/shared/export_m2_redex_oracle.rkt`: 355 assertions pass
  (the prior 113 plus 242 new assertions). New controls exercise both actual
  producer paths with well-scoped, alpha-renamed and omitted-Host forms;
  illegal uses of each new binder in S, read/o in C1, x in D, undeclared free
  names, and valid outer-name shadowing. An expander trap verifies that these
  unsupported contexts never reach target expansion.
- `bash pilot/lean/check-m2.sh`: exit 0. Source-contract schema/evidence checks,
  all 21 production corruption controls (the prior 18 plus category/stage/reason
  mutations) and both positive refinements pass. The unchanged A1 controls and
  227/227 successful-output typing proofs pass; independent parity remains
  74/195 with 74 term/site matches and zero differences. Log:
  `/tmp/f01-a2-m2.casDWp.log`.
- Both existing migration checks and the new
  `python3 pilot/shared/build_f01_a2_migration.py` pass. Documentation and
  full-diff whitespace checks pass.

The unaffected root/M1 literal reports from 8c14eaa were accepted rather than
rerun. Their existing growth warning and uninstrumented full-wall limitation
remain; no performance baseline was reset. All runtime/source-family/replay,
event/quotient, semantic eligibility, RR factoring and Presuppose F1 boundaries
remain unchanged. This is a local successor for focused Astra re-review, not
review clearance, push or merge authority.
